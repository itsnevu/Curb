{
  "name": "@curb/policy-engine",
  "version": "0.2.0",
  "description": "Curb's policy evaluation engine — internal to the gateway and control plane",
  "license": "MIT",
  "//": "Private for now: the servers that use it ship as Docker images, and agents talk to Curb over HTTP via @curb/sdk. Publishing this would commit us to its API.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@curb/shared": "workspace:*",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "ioredis-mock": "^8.13.1"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ]
}
