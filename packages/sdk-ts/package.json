{
  "name": "@curb/sdk",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@curb/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "vitest": "^2.0.0"
  }
}
