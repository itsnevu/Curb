{
  "name": "curb",
  "private": true,
  "version": "0.0.1",
  "description": "Curb — control plane keamanan untuk AI agent (cost/loop breaker + guardrail)",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "up": "docker compose up -d",
    "down": "docker compose down"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "tsx": "^4.16.0"
  }
}
